# LinkedIn Job Finder — Python dependencies
# Install: pip install -r requirements.txt
# After install, also run: playwright install chromium

python-dotenv>=1.0.0
openai>=1.0.0
playwright>=1.40.0
rich>=13.0.0